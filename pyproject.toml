[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[project]
name = "herald"
version = "0.1.0"
description = "Site-scale semantic navigation framework"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "requests>=2.28",
    "shapely>=2.0",
    "pyproj>=3.6",
    "numpy>=1.26",
    "pydantic>=2.0",
    "pillow>=10.0",
    "tqdm>=4.66",
]

[dependency-groups]
dev = [
    "pytest>=8.0",
]
vlm = [
    "langchain>=0.3",
    "langchain-core>=0.3",
    "langchain-ollama>=0.2",
]
viz = [
    "folium>=0.18",
]
viewer = [
    "rerun-sdk>=0.20",
]

[tool.uv]
default-groups = ["dev", "viz", "vlm"]

[tool.setuptools.packages.find]
where = ["."]
include = ["herald*", "services*", "utils*"]

[tool.pytest.ini_options]
testpaths = ["tests"]
